# Filtered by Maven at build time (see resources/filtering in pom.xml).
# Read by com.intersystems.kafka.connect.iris.Version at runtime so
# Connector.version() reports the real build version instead of a
# hand-maintained string that drifts from the POM.
version=${project.version}
